#
# The standard glideinWMS templates assume LSB locations
# This config file restores some of those defaults
#
CERTIFICATE_MAPFILE= $(RELEASE_DIR)/certs/condor_mapfile

GSI_DAEMON_CERT =  $ENV(HOME)/.globus/hostcert.pem
GSI_DAEMON_KEY  =  $ENV(HOME)/.globus/hostkey.pem
